Sources for the plugin `plugin.applovinMax`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax" = {
            publisherId = "com.solar2d",
        },
    },
}
```
